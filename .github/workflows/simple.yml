name: simple try
on: [push]
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
    steps:
    - uses: actions/checkout@v3
    - name: Run my funny commands
      run: |
        pwd
        ls
        echo abcdefg
        uname -a
